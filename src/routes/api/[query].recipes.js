export async function get({params}) {
    //console.log({ params, fetch });
    fetch
    return {
        body: {
            recipes:[
            'cilantro',
            'perejil'
            ]}
    }
}